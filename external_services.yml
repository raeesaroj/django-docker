version: '3'

services:
  psql-11:
    image: mdillon/postgis:11-alpine
    env_file: .env
    volumes:
      - postgres-data:/var/lib/postgresql/data
  redis:
    image: redis:alpine

volumes:
  postgres-data:
